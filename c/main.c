#include "utils/disjoint_set.h"
#include "utils/hash_table.h"
#include "utils/heap.h"
#include "utils/kmp_search.h"
#include "utils/linked_list.h"
#include "utils/tree.h"
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// #include problems from 'problems' folder and test here

// int compare(const void *a, const void *b) { return (*(int *)a - *(int *)b); }

#define ll long long
#define ull unsigned long long

int main() {
  // unsigned long SZ = 1000;
  // char *key1 = "Tien Anh";
  // char *key2 = "Tien Bro";
  // hash_table *table = hash_table_create(SZ);
  // hash_table_set(table, key1, "dep trai nhat the gioi!");
  // printf("%s: %s\n", key1, hash_table_get(table, key1));
  // printf("%s: %s\n", key2, hash_table_get(table, key2));
  // hash_table_free(table);

  // heap *h = heap_create(comp, 1000);
  // heap_push(h, 10);
  // heap_push(h, 12);
  // heap_push(h, 9);
  // heap_push(h, 3);
  // heap_push(h, 25);
  // while (heap_length(h) > 0) {
  //   printf("%d ", heap_pop(h));
  // }
  // heap_free(h);

  // printf("%d", kmp_search("abcderft", 8, "cde", 3));

  printf("\n");
  return 0;
}
